import { DndContentDirective } from './dnd-content.directive';

describe('DndContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DndContentDirective();
    expect(directive).toBeTruthy();
  });
});
