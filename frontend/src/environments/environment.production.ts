/*export const environment = {
    production: false,
    frontendPort: process.env['NG_FRONTEND_IP_ADDRESS'] || '4200',
    hostServerUrl: process.env['NG_HOST_SERVER_URL'] || 'http://host-server-url'
};*/

export const environment = {
    production: false,
    frontendPort: 4200,
    hostServerUrl: 'http://localhost:8080'
};
